import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "PC Builder Pro - Ultimate Gaming PC Builder",
  description:
    "Build your dream gaming PC with our intelligent recommendations, compatibility checking, and performance optimization tools.",
  generator: "v0.app",
  keywords: "PC builder, gaming PC, custom PC, computer builder, gaming setup, PC components",
  authors: [{ name: "PC Builder Pro" }],
  themeColor: "#f59e0b",
  viewport: "width=device-width, initial-scale=1",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable} antialiased`}>
        <Suspense fallback={<div>Loading...</div>}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
